# PUFp OPV Demo

This example is the future home for a small OPV-oriented workflow:

1. recognize polymer units
2. generate PUFp features with `purs fingerprint`
3. align features with targets
4. run a baseline ML model

The current CSV files are placeholders that define the expected data layout.
