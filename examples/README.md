# Examples

This directory is the main public entry for the package.

The released repository keeps one public example:

- `opecm_standard_demo/`

That example uses one standard sample table:

- `data/testing/opecm_paper54_tasks.csv`

If you want to understand the package, start there first.
