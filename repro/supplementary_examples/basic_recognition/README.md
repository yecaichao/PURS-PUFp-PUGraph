# Basic Recognition Demo

Use this example to validate the future unified `recognize` and `classify` commands.

Expected purpose:

- verify input loading
- verify duplicate-name handling
- verify standard output file generation

Example command:

```bash
purs recognize --input-csv repro/supplementary_examples/basic_recognition/input.csv --output-dir output/basic
```
