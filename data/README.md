# Data

The public package keeps one standard sample dataset:

- `data/testing/opecm_paper54_tasks.csv`

This table is the unified source for the released example workflow.

It is intentionally small and self-contained enough to support:

- `PURS` recognition checks
- `PUFp` classification and regression demonstrations
- `PUGraph` input preparation and wrapped graph workflow checks
